# dockerWorkspace

Attempt to build a reproducible, low-friction development workspace using a containerized workspace

The `.env` must be populated with all API keys passed to Express in `docker compose` to interact with outbound APIs.

```
docker compose up --build
```
